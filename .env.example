# Backend base URL used by the contact form.
# Local dev with the Express server (localhost:5000) running:
VITE_API_URL=http://localhost:5000
# Production: set VITE_API_URL in your CLIENT Vercel project to the deployed
# backend URL, e.g. VITE_API_URL=https://your-backend-project.vercel.app
